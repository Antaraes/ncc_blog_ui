import { FC } from 'react';
import AddCategoriesPage from './AddCategoriesPage';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <>
      <AddCategoriesPage />
    </>
  );
};

export default page;
