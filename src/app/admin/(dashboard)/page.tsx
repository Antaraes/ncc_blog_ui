import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <p className="text-3xl font-bold">Dashboard</p>
    </div>
  );
};

export default page;
